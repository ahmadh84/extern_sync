function allOpticalFlow
data_dir = '~/data/Images/SegTrack_201102';

videoName = 'birdfall2';
computeOpticalFlow(videoName, data_dir);

videoName = 'cheetah';
computeOpticalFlow(videoName, data_dir);

videoName = 'girl';
computeOpticalFlow(videoName, data_dir);

videoName = 'monkeydog';
computeOpticalFlow(videoName, data_dir);

videoName = 'parachute';
computeOpticalFlow(videoName, data_dir);

videoName = 'penguin';
computeOpticalFlow(videoName, data_dir);

videoName = 'penguin_rszd';
computeOpticalFlow(videoName, data_dir);


data_dir = '~/data/Images/SegTrack_2013';

videoName = 'bird_of_paradise_rszd';
computeOpticalFlow(videoName, data_dir);

videoName = 'bmx_rszd';
computeOpticalFlow(videoName, data_dir);

videoName = 'drift_rszd';
computeOpticalFlow(videoName, data_dir);

videoName = 'frog';
computeOpticalFlow(videoName, data_dir);

videoName = 'hummingbird_rszd';
computeOpticalFlow(videoName, data_dir);

videoName = 'monkey';
computeOpticalFlow(videoName, data_dir);

videoName = 'soldier';
computeOpticalFlow(videoName, data_dir);

videoName = 'worm';
computeOpticalFlow(videoName, data_dir);

end

